# CLAUDE.md · 给 AI 的协作契约（模板 v2）

> 这是 **AI 每次进入本项目的第一份必读文件**。
> 复制本模板到项目根目录,把 `<>` 占位符替换为实际内容。
> 根据项目 Level 删减不适用的部分。

---

## 项目身份

- **项目名**：<项目名>
- **领域**：<理想态与评测 / 学术论文 / 产品规范 / 运营 SOP / 品牌体系 / 培训课程 / 法律合规 / 团队 Wiki / 开源项目 / UX 研究 / 数据分析 / 其他>
- **Level**：<L1 Assist / L2 Remember / L3 Operate / L4 Guard>
- **当前阶段**：<MVP / 稳定迭代 / 重构中 / 维护期>
- **Owner**：<name>

## 项目目的（一句话）

<为什么做这个项目,终极交付是什么>

---

## 目录约定

```
<project>/
├── 00-charter/              宪章层 · 必选
├── 10-<domain>/             领域核心 · 必选
├── 20-specs/                规范层 · 可选
├── 30-resources/            资源层 · 可选
├── 40-process/              过程层 · 必选
│   ├── inbox/               唯一输入入口
│   ├── decisions/           ADR（L3+）
│   └── rule-history.md      外部规则归档
└── 99-raw/                  原始素材 · 可选,只读
```

---

## 每次必读清单（按优先级）

> AI 进入项目后,必须先读以下文件建立上下文。**不在此清单的按需查阅**。
> 控制在 3-5 份,过长会读不完。

1. `CLAUDE.md`（本文件）
2. `00-charter/01-项目背景与目标.md`
3. `00-charter/02-核心概念词表.md`
4. `10-<domain>/00-总则.md`
5. `20-specs/00-总览.md`（如有）

---

## 输入分拣表

| 用户给的输入类型 | 放哪 | AI 动作 |
|---|---|---|
| 原始材料（docx / 音频 / 他人给的）| `99-raw/` | 只存档,**不编辑** |
| 会议纪要 / 群聊消息 | `40-process/inbox/YYYY-MM-DD-主题.md` | 提炼行动项 + 关联文件 |
| 临时灵感 / 待办 | `40-process/inbox/` | 就地整理,问 Owner 是否落地 |
| 明确的规则变更 | 改 `20-specs/` + `30-resources/*-changelog.md` + 联动检查 | |
| 方向性决策（L3+）| `40-process/decisions/ADR-<NNNN>.md` | 先起草 ADR,Owner Accept 再落地 |

---

## 治理机制（按 Level 启用）

### L1 · Assist

不需要专门机制,直接协作即可。

### L2 · Remember

- ✅ inbox 分拣：所有散乱输入先进 inbox 再分发
- ✅ 概念词表维护：新术语进 `02-核心概念词表.md`

### L3 · Operate

L2 所有 + 以下：

- ✅ 方向性决策写 ADR
- ✅ SSoT 变更（CSV/规则）写 changelog
- ✅ 改动后执行联动检查（见下节）

### L4 · Guard

L3 所有 + 以下：

- ✅ 影响评估：影响评分/下游的改动必须量化漂移
- ✅ 变更审批：改 SSoT 必须 Owner 明确 Accept
- ✅ 联动检查报告存档
- ✅ 定期治理健康度检查（建议月度）

---

## 联动检查清单（L3+ 必读）

**每次改动下列对象后,AI 必须主动执行全局搜索并报告**：

| 改了什么 | 必检查 |
|---|---|
| 改 SSoT 数据（CSV / JSON / BibTeX）| 追加 changelog / 更新架构说明 / 检查所有引用 / 重生派生物 |
| 改核心词表 | 全局搜旧名,更新活引用,保留历史陈述 |
| 改规则阈值 | (L4) 评估打分漂移 / 给出预期影响 |
| 新增 / 改名维度 | 更新本文件必读清单 / 总则索引 / 相关 README / 既有 ADR 演化记录 |
| 删除标签 / 文件 | 历史陈述保留,活引用必改 |

**报告格式**见 `references/cross-ref-check.md`。

---

## 什么时候征询用户（vs 直接执行）

### 直接执行（不打断用户）

- 改错别字 / 修格式 / 调排版
- 补交叉引用、内部链接
- 按已明确规范生成 / 重新生成派生物
- 回答问题、写总结

### 征询用户（必须 ask）

- 方向性决策：P 级分档、权重调整、维度增减、架构重构
- 涉及 SSoT 数据的删除 / 合并 / 拆分
- 新建 ADR 前的选项对比
- 用户意图有 2+ 种合理解读
- (L4) 任何改 SSoT 的动作

---

## 项目内专属约定

<写在这里：本项目独有的术语 / 规范 / 命令 / 隐藏假设>

**示例**：
- 打分规则以 `20-specs/00-总览.md §三` 为准,其他文档引用不复制
- 存量数据不变,只改字典（除非 ADR 明说要回溯）
- `99-raw/` 下文件**永远不可编辑**,AI 触发保护规则

---

## 待观察与风险

<列出当前悬而未决的事项 / 已知技术债 / 接下来要对齐的议题>

---

## 本契约更新记录

| 日期 | 变更 | 原因 |
|---|---|---|
| YYYY-MM-DD | 初版 | 项目启动 |
| YYYY-MM-DD | <变更> | <原因> |
